"""FastAPI inference server for Parkinson's Disease detection."""

# from fastapi import FastAPI, UploadFile, File, HTTPException
# from pydantic import BaseModel
# from src.deployment.predictor import PDPredictor

# app = FastAPI(
#     title="Parkinson Speech AI — Inference Server",
#     description="REST API for PD detection from speech recordings.",
#     version="0.1.0",
# )

# TODO: Implement after model training is complete.
# Endpoints to implement:
#   POST /predict   — accepts audio file, returns PD probability
#   GET  /health    — returns server health status
#   GET  /model-info — returns loaded model metadata
